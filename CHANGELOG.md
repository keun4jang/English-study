# Changelog

D-log의 모든 주요 변경 사항을 기록합니다.
형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/)를 따르고,
버전은 [Semantic Versioning](https://semver.org/lang/ko/)을 따릅니다.

<!-- next-version -->

## [0.13.2] - 2026-08-20

### 추가 (사전 보강)

- 움직임·일상 동사를 넣었습니다 — 뛰다, 달리다, 자르다, 타다, 오르다, 입다, 열다,
  수영하다, 조깅하다.
- 교통수단(지하철·버스·택시·기차·비행기)과 촬영 관련 명사(영상·셀카)를 넣었습니다.

이제 되는 문장들:

- 오늘 공원에서 뛰었어 → I went for a run at the park today.
- 오늘 머리 잘랐어 → I cut my hair today.
- 오늘 지하철 탔어 → I took the subway today.
- 아침에 수영했어 → I went swimming in the morning.

### 수정

- 사전에 같은 단어가 두 번 들어가 있었습니다(영상). 뒤엣것은 영원히 쓰이지 않아서,
  고쳐야 할 때 눈에 안 보이는 쪽을 고치게 됩니다. 중복을 없애고 테스트로 막았습니다.

## [0.13.1] - 2026-08-20

### 수정 (문장을 반만 읽고 엉뚱한 영어를 만들던 문제)

"오늘 여의도 한강 공원을 뛰면서 영상과 사진 촬영을 했어" 를 쓰면
**"I had a shoot in [뛰면서] today."** 가 나왔습니다.

원인은 **알아본 단어를 조용히 버린 것**이었습니다. 한강·공원·영상·사진을 전부 사전에서
찾아 놓고도 "자리가 이미 찼다"는 이유로 버린 뒤, 남은 「뛰면서」를 장소 자리에 끼워
넣었습니다. 그러고도 결과는 성공으로 나왔어요.

- **이제 다 읽지 못한 문장은 예시를 만들지 않습니다.** 버려야 할 단어가 하나라도 있거나,
  다룰 수 없는 다른 절(-면서, -고, -는데, -지만, -아서 …)이 있으면 만들지 않습니다.
- 대신 **무엇까지 읽었는지 보여주고 되묻습니다.**
  "한 문장에 여러 이야기가 담겨 있어서 절반만 읽고 엉뚱한 영어를 만들 뻔했어요.
   그건 보여드리지 않을게요. 한 번에 하나씩 나눠서 써 주시면 만들 수 있어요."
  아래에 읽은 부분(오늘 · 한강 · 공원 · 영상 · 사진)도 함께 보여줍니다.
- **조사가 없어 역할을 모르는 말은 장소 자리에 넣지 않습니다.** 한국어에서 장소는 거의 항상
  "에/에서"를 달고 나오기 때문에, 조사 없는 말을 장소로 넣으면 십중팔구 틀립니다.
  ("in [뛰면서]", "in [먹고]" 가 그렇게 나왔습니다)

멀쩡한 단문은 그대로 만듭니다 — 실제 문장 30개로 확인했고 29개가 정상 동작합니다.
이 판정이 되는 문장까지 막지 않도록 테스트로 고정했습니다.

## [0.13.0] - 2026-08-20

### 추가 (대화 지우기)

- **AI 대화 화면 오른쪽 위에 휴지통 버튼**이 생겼습니다. 하던 대화를 통째로 지울 수 있어요.
  지우기 전에 몇 개의 문장이 사라지는지 알려주고, **이 대화로 이미 만든 일기는 그대로
  남는다**는 것도 함께 알려줍니다.
- 홈의 "이어서 하기" 카드에도 **지우기**가 생겼습니다. 안 이어갈 대화나 쓰다 만 일기를
  들어가지 않고 바로 정리할 수 있어요. 지울 방법이 없으면 그 카드가 계속 따라다닙니다.

### 수정 (백업 파일 내보내기가 안드로이드에서 아예 안 되던 문제)

- **백업 공유가 안드로이드에서 한 번도 성공한 적이 없었습니다.** 크롬은 공유할 수 있는
  파일 형식을 허용목록으로 제한하는데 `.json`과 `application/json`이 **양쪽 목록 모두에
  없어서** 매번 거부되고 있었습니다. 파일 형식을 `.txt` / `text/plain`으로 바꿨습니다.
  내용은 그대로 JSON이고, 가져오기는 확장자가 아니라 내용을 보고 판별하므로 그대로 됩니다.
- **아이폰 홈 화면 앱에서는 파일 내려받기를 시도하지 않습니다.** 그 환경에서 다운로드를
  걸면 "Open in…" 화면에 갇혀 앱을 강제 종료해야만 빠져나오는 문제가 있습니다.
  대신 공유하기와 복사하기만 씁니다.
- 공유할 때 제목을 함께 넘기던 것을 뺐습니다. 아이폰 공유 화면에서 제목이 별도 항목으로
  끼어들면서 파일 대상이 깨지는 경우가 있습니다.

### 변경 (성공을 단정하지 않습니다)

- **"저장됐어요"라고 단정하지 않습니다.** 공유가 끝나도 그건 "고른 앱에 넘겼다"는 뜻이지
  드라이브 업로드가 끝났다는 뜻이 아니고, 다운로드는 아예 성공을 알 방법이 없습니다.
  여기서 단정하면 사용자가 백업된 줄 알고 앱 데이터를 지울 수 있어서, 이 앱에서 나올 수
  있는 최악의 실패가 됩니다. "고른 앱에서 저장이 끝났는지 한 번 확인해 주세요"로 바꿨습니다.
- 버튼 설명에 "D-log가 직접 어딘가에 올리는 건 아니에요"를 적었습니다.

### 수정 (id가 비어 두 항목이 같은 값을 갖던 문제)

- 기기에서 `crypto.randomUUID`를 못 쓰면 id가 빈 값이 될 수 있었습니다. 그러면 일기나
  대화 두 개가 같은 id를 갖게 되고, **하나를 지울 때 다른 것까지 지워집니다.**
  난수로 직접 UUID를 만드는 대체 경로를 넣었습니다.

## [0.12.1] - 2026-08-20

### 수정 (백업 파일 내보내기를 눌러도 아무 반응이 없던 문제)

- **버튼을 눌러도 아무 일도 일어나지 않았습니다.** 공유 기능이 실패할 때 나오는
  `NotAllowedError`를 "사용자가 공유를 취소함"으로 잘못 처리하고 있었습니다.
  취소로 보면 안내도 안 뜨고, 파일 내려받기 대안으로도 넘어가지 않아서 버튼이 죽은 것처럼
  보였습니다. 사용자가 직접 창을 닫은 경우(`AbortError`)만 취소로 다루도록 고쳤습니다.
- **공유가 안 되면 이제 반드시 파일 내려받기로 넘어갑니다.** 그마저 안 되는 기기에서는
  "JSON 복사하기로 저장해 주세요"라고 안내합니다. **어떤 경우에도 아무 반응 없이 끝나지
  않습니다.**
- 눌렀을 때 화면 상태를 먼저 바꾸던 순서를 바꿨습니다. 화면을 먼저 그리면 브라우저가 이걸
  "사용자가 방금 누른 동작"으로 보지 않아서 공유 창이 안 열릴 수 있습니다.
- 내려받기로 저장했을 때 **파일이 어디에 무슨 이름으로 있는지** 알려줍니다.
  ("내 파일 → 다운로드에 D-log-backup-2026-08-20.json 로 있어요")

이 다섯 가지 상황(공유 성공 / 사용자 취소 / 공유 실패 / 공유 없음 / 둘 다 막힘)을
테스트로 고정했습니다.

## [0.12.0] - 2026-08-20

### 추가 (백업을 구글 드라이브·메일에 저장할 수 있어요)

- 설정 → 계정에 **"백업 파일 내보내기"** 버튼이 생겼습니다. 누르면 백업 파일을 만들어
  **폰의 공유 화면**으로 보냅니다. 거기서 구글 드라이브에 저장하거나, 메일·카카오톡으로
  나에게 보내거나, 파일 앱에 넣을 수 있어요.
- 공유 화면이 없는 기기(주로 PC)에서는 **파일로 내려받습니다.** 받은 파일을 드라이브에
  올리거나 메일에 첨부하면 됩니다.
- 예전 방식인 **"JSON 복사하기"도 그대로** 남겨 뒀습니다. 위 두 가지가 안 되는 환경을 위한
  마지막 방법이에요.

**구글 드라이브와 직접 연동하지 않았습니다.** 그러려면 구글 클라우드 프로젝트와 OAuth 키가
필요한데, 새 키를 만들지 않는다는 원칙에 어긋나고 나중에 비용이 생길 여지가 됩니다.
공유 화면으로 넘기는 방식은 드라이브만이 아니라 **메일·카카오톡·아이클라우드·파일 앱까지
전부** 되고, 계정 연결도 필요 없습니다.

### 수정

- **백업 파일 이름에 한글을 쓰면 파일 이름이 통째로 사라졌습니다.** 크롬이 한글이 들어간
  다운로드 이름을 무시하고 확장자도 없는 `download`로 저장했습니다. 나중에 파일 목록에서
  찾을 수도, 가져오기에서 알아볼 수도 없게 되는 문제였어요.
  이름을 `D-log-backup-2026-08-20.json` 형태로 바꾸고 테스트로 막아 뒀습니다.

### 변경

- 백업 안내 문구를 고쳤습니다. 앱을 지우거나 폰을 바꾸면 일기가 사라진다는 것과,
  백업 파일에 일기 내용이 그대로 들어 있으니 아무 데나 올리면 안 된다는 것을 적었습니다.

## [0.11.2] - 2026-08-20

### 변경 (손글씨 폰트를 뺐습니다)

- **오늘의 편지 질문이 잘 안 읽혔습니다.** 손글씨 폰트(Gaegu)를 쓰고 있었는데,
  매일 읽는 문장에서는 감성보다 읽기가 먼저입니다. 다른 화면과 같은 본문 폰트(SUIT)로
  되돌렸습니다.
- 같은 폰트를 쓰던 나머지 세 곳도 함께 되돌렸습니다 — **쓰기 탭 제목**(홈과 똑같은
  크기·폰트라 같은 문제가 있었어요), **빈 화면 안내**, **온보딩 소개 문장**.
- 질문 문구는 `prompt` 토큰(21px / 줄간격 32 / 600)으로 정리했습니다. 화면에 크기를
  박아 넣지 않아서 글자 크기 설정(작게·크게)이 그대로 적용됩니다.

### 변경 (앱이 143 KiB 가벼워졌습니다)

- 쓰는 곳이 없어진 폰트를 계속 실을 이유가 없어서 파일도 뺐습니다.
  첫 로딩에 받던 폰트가 641 KiB → 498 KiB로 줄었습니다.
- 되돌리고 싶으면 이전 기록에 폰트 파일과 서브셋 스크립트가 그대로 남아 있습니다.

## [0.11.1] - 2026-08-20

### 변경 (홈을 "오늘 일기 쓰기" 하나로 줄였습니다)

홈에 목표 진행바, 연속 기록, 그날의 기억, 복습 알림, 연습 알림, 학습 통계·단어장 버튼,
검색 버튼이 전부 올라가 있었습니다. 하나하나는 쓸모가 있었지만 한 화면에 모아 놓으니
지금 뭘 하라는 화면인지 알 수 없었습니다.

홈에 남긴 것은 오늘 쓰는 데 필요한 것뿐입니다.

1. 오늘이 며칠인지 (날짜 + 인사말)
2. 쓰다 만 게 있는지 (초안 또는 진행 중 대화가 있을 때만, **하나만**)
3. 무엇에 대해 쓸지 (오늘의 편지)
4. 오늘 쓴 것

**없앤 게 아니라 제자리로 옮겼습니다.**

- 그날의 기억 · 검색 → **달력** (되돌아보는 화면이 달력입니다)
- 오늘의 목표 · 연속 기록 → **학습 통계** (설정 → 학습 통계 보기)
- 다시 말해보기 · 단어장 복습 → **쓰기 탭** (복습할 개수가 카드 설명에 나옵니다)

빈 상태 안내에 있던 "AI 친구에게 이야기하기" 버튼도 뺐습니다. 바로 위 오늘의 편지 카드에
같은 버튼이 이미 있어서 두 번 물어보는 꼴이었습니다.

## [0.11.0] - 2026-08-20

### 추가 (다른 일기 앱들에 있는 것들)

- **그날의 기억** — 홈에서 "1년 전 오늘" 쓴 일기를 보여줍니다. 1년을 안 채웠어도
  1·3·6개월 전 오늘도 함께 봐서, 시작한 지 한 달만 지나도 뭔가 뜹니다.
  하루에 최대 두 장까지만 — 홈이 과거로 뒤덮이면 안 되니까요.
- **감정 흐름** — 학습 통계에 최근 30일 감정 막대가 생겼습니다. 감정은 이미 일기마다
  저장하고 있었는데 어디서도 되돌려 보여주지 않았습니다.
  점수나 평균은 만들지 않습니다. 좋았던 날·평범한 날·힘들었던 날이 며칠인지만 보여줘요.
  점수를 붙이면 점수를 올리려고 감정을 고르게 되고, 그러면 일기가 아니니까요.
- **앱 잠금** — 앱을 열 때 숫자 네 자리를 물어봅니다. 폰을 잠깐 빌려줄 때를 위한 거예요.
  PIN은 그대로 저장하지 않고 PBKDF2 해시만 남깁니다 (기기 안에서 계산, 네트워크 없음).

### 변경

- **잠금을 잊어도 일기를 잃지 않습니다.** 일기 본문을 암호화하지 않기 때문에 잠금 화면에서
  끄고 들어갈 수 있습니다. 본문까지 암호화하면 PIN을 잊는 순간 일기가 영영 사라지는데,
  일기 앱에서는 지켜주는 것보다 잃는 게 큽니다. 설정 화면에 이 점을 그대로 적었습니다.

### 수정

- **개인정보 안내가 사실과 달랐습니다.** "AI 교정을 사용하면 입력한 텍스트가 서버와 AI
  제공자에 전송될 수 있어요"라고 적혀 있었는데, 지금은 교정도 예시도 전부 기기 안에서
  계산합니다. 쓴 글은 어디로도 나가지 않아요. 안내를 사실대로 고쳤습니다.

## [0.10.0] - 2026-08-19

### 추가 (사전을 크게 넓혔습니다)

- 일상·직장·학교·외출·음식·감정 여섯 갈래로 실제 쓰는 문장을 훑어서 표제어를 늘렸습니다.
  **명사 150개, 활동 77개, 형용사 55개**가 들어왔습니다. 제안된 영어는 전부 다시
  검증을 거쳐, 어색하거나 틀린 66개는 뺐습니다.
- 이제 되는 문장들:
  "오늘 대구에서 촬영을 했어" → "I had a shoot in Daegu today."
  "어제 야근했어" → "I worked late yesterday."
  "오늘 너무 짜증났어" → "I was so annoyed today."
  "저녁에 회식 했어" → "I had a work dinner in the evening."
  "오늘 진짜 정신없었어" → "It was so hectic today."
- **지역·나라 이름 47개**를 넣었습니다 (서울·부산·대구·강남·홍대·제주·일본…).

### 수정 (지명은 at이 아니라 in입니다)

- 검증 단계에서 잡힌 문제입니다. 지명을 그냥 명사로 넣으면 "I went to Busan"은 맞지만
  "~에서"가 들어가는 순간 **"at Busan"**이 되어 전부 틀린 문장이 됩니다.
- 장소마다 전치사를 따로 두었습니다. 도시·동네·나라는 in, 건물·가게는 at입니다.
  사전에 없는 장소도 in을 씁니다 — 모르는 장소 이름은 대개 지명이기 때문입니다.

### 수정 (놓치던 말투)

- **"촬영을 했어"처럼 조사가 낀 '명사 + 하다'**를 못 알아들었습니다. 제일 흔한 형태인데
  놓치고 있었습니다. 이제 "운동했어 / 운동 했어 / 운동을 했어"가 모두 같습니다.
- **"등산 갔어"처럼 '가다'로 말하는 활동**을 못 알아들었습니다. 여행·등산·캠핑·출장·
  데이트·쇼핑처럼 '갔어'가 더 자연스러운 활동을 따로 처리합니다.
- 미래형에서 go가 두 번 나오던 것을 고쳤습니다.
  "I'm going to go on a trip" → "I'm going on a trip".

### 변경 (문장 만들기 선택지 정리)

- 사전이 커지면서 한 단계에 칩이 **91개**까지 늘어났습니다. 고르는 화면이 벽이 되면
  "고르기만 하면 된다"는 이 기능의 이유가 사라집니다. 자주 쓰는 것만 남겨
  단계당 최대 16개로 줄였습니다. 나머지는 한국어로 직접 써서 예시를 받으면 됩니다.

## [0.9.1] - 2026-08-19

### 수정 (한글 도우미가 틀린 문장을 자신 있게 보여주던 문제)

- **조사가 알려주는 자리를 버리고 있었습니다.** "오늘 대구에서 촬영을 했어"에서 `에서`는
  대구가 장소라고 알려주는데, 그 정보를 버리고 모르는 단어 목록에서 앞에서부터 꺼내
  목적어 자리에 넣었습니다. 그래서 장소가 목적어가 되고 진짜 목적어(촬영)는 사라져
  **"I did [대구에서] today."** 가 나왔습니다.
- 이제 조사가 알려주는 자리를 지킵니다.
  "오늘 부산에서 라면 먹었어" → "I ate ramen at [부산] today."
- **모르는 단어의 조사를 떼지 않아서** `[대구에서]`처럼 조사가 붙은 채로 화면에 나왔습니다.
  이제 떼어냅니다. 다만 `떡볶이`가 `떡볶`으로 잘리지 않도록, 남는 말이 2글자 이상이고
  조사의 받침 규칙에 맞을 때만 뗍니다.
- **뜻이 거의 안 남는 문장을 그냥 내놨습니다.** 아무 뜻 없는 '하다'인데 목적어까지 모르면
  "I did … today"만 남는데, 이걸 보여주면 앱이 뭔가 이해한 것처럼 보입니다. 이제 내놓지
  않고 문장 만들기로 안내합니다.
- **띄어 쓴 '명사 + 하다'를 못 알아들었습니다.** "오늘 운동 했어"(띄어쓰기)가 실패했습니다.
  맞춤법은 붙여 쓰는 게 맞지만 실제로는 띄어 쓰는 사람이 훨씬 많습니다. 이제 같게
  처리합니다. 사이에 낀 부정어도 잃지 않습니다 ("오늘 운동 안 했어").

### 추가 (이름은 소리 나는 대로)

- **국어의 로마자 표기법**을 넣었습니다. 지역·가게·사람 이름은 사전을 아무리 키워도 다
  담을 수 없고, 애초에 번역이 아니라 표기의 문제입니다. 대구 → Daegu, 강남 → Gangnam.
- 문장 안에 몰래 넣지는 않습니다 — 이름이 아닐 수도 있어서 `[대구]`로 두고 옆에
  제안만 합니다. 문장을 못 만들었을 때도 이름 표기는 알려줍니다.

### 수정 (교정 엔진이 놓치던 흔한 오류)

- **시간 표현을 주어로 쓴 문장을 못 잡았습니다.** 한국어는 주어를 생략하니까
  "오늘 촬영했어"가 "today is shooting in daegu"로 나오는데, `Today is ~`는
  "Today is my birthday."처럼 맞는 문장이기도 해서 그냥 통과했습니다.
  이제 고칩니다 — "Today I'm shooting in daegu."
- 날씨는 주어가 it입니다 — "Today is raining." → "Today it's raining."
- 멀쩡한 문장("Today is my birthday", "Today is shooting day")은 건드리지 않습니다.
  -ing 동사를 목록으로 제한하고 그 뒤가 전치사·부사로 이어질 때만 고칩니다.

## [0.9.0] - 2026-08-19

### 추가 (한국어로 말하면 예시 문장을 보여줍니다)

- **한국어로 쓰면 영어 예시를 만들어 줍니다.** "오늘 친구랑 카페 갔어"라고 쓰면
  "I went to a cafe with a friend today."를 보여주고, 그대로 입력창에 넣거나 소리로
  들어볼 수 있습니다. 대화 화면과 직접 쓰기 화면 둘 다에서 됩니다.
- **골라서 문장 만들기.** 사전이 못 알아듣는 문장이면 틀을 주고 고르게 합니다.
  "언제 / 무엇을 먹었어요 / 누구랑"을 고르면 문장이 완성됩니다. 여섯 가지 틀이 있고,
  고르는 동안 완성될 문장이 아래에 계속 보입니다.
- 대화 중 아무 때나 **"문장 만들기"** 버튼으로 열 수 있습니다.

### 변경

- 이 기능도 **인터넷 번역기를 쓰지 않습니다.** 기기 안의 사전과 문장 틀로만 만들어서
  비용이 없고 비행기 안에서도 됩니다. 대신 모든 문장을 만들지는 못하는데,
  **못 만들면 못 만든다고 말합니다.** 그럴듯한 가짜 번역을 지어내면 사용자는 그게 틀린 줄
  모르고 일기에 그대로 쓰게 되기 때문입니다.
- 사전에 없는 단어는 `[떡볶이]`처럼 대괄호로 남기고 "이 단어는 아직 몰라요"라고 알립니다.
- 입력창 안내 문구가 "영어로 말해보세요 — 한국어로 써도 괜찮아요"로 바뀌었습니다.

### 수정 (한국어가 영어 일기로 저장되던 문제)

- **한국어로 쓴 문장이 영어 문장으로 취급됐습니다.** 앱은 입력 언어를 확인하지 않고
  설정된 학습 언어로 단정했기 때문에, "오늘 친구랑 카페 갔어"에 마침표만 붙여서
  **"잘 썼어요"로 표시**했습니다.
- 그리고 그 한국어가 **그대로 영어 일기 본문에 저장**됐습니다. 일기 본문은 사용자가 쓴
  문장을 이어붙여 만드는데, 한국어를 걸러내지 않았습니다.
- 이제 한국어로 쓰면 보내지 않고 예시를 먼저 보여줍니다. 대화 횟수도 깎이지 않습니다.
  이전 대화에 한국어가 남아 있는 경우에도 일기를 만들 때 한 번 더 걸러냅니다.
- 주제 사전이 영어·일본어 패턴만 있어서 한국어에는 매번 같은 답만 나오던 것도 함께
  해결됩니다 (이제 한국어는 대화로 보내지 않습니다).

## [0.8.0] - 2026-08-19

### 추가 (손글씨 폰트로 만든 아기자기한 감성)

- **손글씨 폰트 Gaegu(개구)를 넣었습니다.** 앱이 사용자에게 말을 거는 자리에만 씁니다 —
  홈의 "오늘의 편지" 질문, 빈 화면 안내 문구, 기록 방식 고르기 제목, 온보딩 소개 문장.
- 반대로 **버튼·설정·교정 카드·통계 숫자·날짜, 그리고 사용자가 쓴 글**에는 쓰지 않습니다.
  손글씨는 읽는 속도가 느려서, 판단하고 조작하는 자리에 쓰면 오히려 불편해집니다.
- 타이포그래피 토큰 `editorial` / `editorialTitle` / `editorialBody`를 추가했습니다.
  Gaegu는 획이 가늘고 글자 폭이 넓어서, 같은 위계라도 한 단계 크게 잡고 줄간격을 넓혔습니다.

### 변경

- 폰트는 여전히 **로컬 파일만** 씁니다. 외부 CDN을 부르지 않으므로 오프라인에서도 뜨고,
  비용이 생길 여지가 없습니다. (SIL OFL 1.1 · 상업적 사용·번들 허용)
- 용량을 위해 **앱에 실제로 쓰인 한글만 남긴 서브셋**을 싣습니다. 전체(775KB) 대신
  131KB입니다. 재생성 스크립트는 `scripts/subset-hand-font.py`입니다.
- 첫 화면에 바로 보이는 Gaegu Bold만 미리 받고, Regular는 필요할 때 받습니다.

### 수정

- 홈의 "오늘의 편지" 질문이 토큰이 아니라 화면 안에 박힌 크기 값(21px)을 쓰고 있었습니다.
  글자 크기 설정(작게/크게)이 이 문장에만 적용되지 않던 문제도 함께 해결됩니다.

## [0.7.6] - 2026-08-19

### 수정 (뒤로가기로 나갈 때 쓰던 내용이 사라지던 문제)

- **폰 뒤로가기(갤럭시 제스처·버튼)가 확인창을 띄우지 않았습니다.** 홈 화면에 설치한
  PWA에서 폰 뒤로가기는 브라우저 뒤로가기로 들어오는데, 앱은 화면 안 뒤로가기 버튼만
  잡고 있었습니다. 그래서 뒤로가기 한 번에 화면이 그냥 닫혔습니다.
- **일기 수정 화면은 확인 자체가 없었습니다.** 임시 저장도 없어서 뒤로가기를 누르면
  고친 내용이 그대로 사라졌습니다.
- 직접 쓰기 화면도 확인 없이 나가졌습니다.

### 변경

세 화면(일기 수정 · 직접 쓰기 · AI 대화)의 나가기 확인을 하나로 맞췄습니다.
어느 화면에서든 순서와 문구가 같습니다.

1. **저장하고 나가기**
2. **저장 안 하고 나가기**
3. **계속 쓰기**

나가면 무엇이 사라지는지(또는 사라지지 않는지)를 화면마다 정확히 적었습니다.

- 일기 수정: "방금 고친 내용은 사라지고, 원래 일기가 그대로 남아요"
- 직접 쓰기: "쓰던 내용은 임시 보관돼요. 홈에서 이어서 쓸 수 있어요"
- AI 대화: "대화는 사라지지 않고, 홈에서 이어서 이야기할 수 있어요"

브라우저에서 실제로 뒤로가기를 눌러 8가지 경우를 확인했습니다 — 확인창이 뜨는지,
'계속 쓰기'로 머물 때 내용이 남는지, 두 번째 뒤로가기에도 다시 뜨는지, 저장 후
뒤로가기가 수정 화면으로 헛돌지 않는지, 고친 게 없으면 확인창 없이 바로 나가는지.

## [0.7.5] - 2026-08-19

화면별 검토 결과를 하나하나 다시 검증해서 남은 것들을 정리했습니다.

### 수정

- **설정 토글 손잡이가 여전히 청록색이던 문제.** 앞 버전에서 색을 지정했지만, 웹에서는
  켜졌을 때 다른 속성(`activeThumbColor`)을 쓰고 그 기본값이 청록색이라 절반만 고쳐져
  있었습니다. 토글을 별도 컴포넌트로 분리하고 이유를 적어 뒀습니다.
- **빈 화면(친구·휴지통·연습)의 안내가 화면 위쪽에 붙어 있던 문제.** 남은 공간을 채우게
  돼 있었는데 실제로는 동작하지 않아, 버전 표기가 화면 한가운데 떠 있었습니다.
- 단어장의 '발음 듣기'가 바로 아래 4지선다 보기 상자와 똑같이 생겨 무엇이 버튼인지
  헷갈리던 문제.
- 일기 상세에서 '내가 쓴 원문 보기'가 카드 안 전체폭 버튼이라 같은 색 상자가 이중으로
  겹쳐 보이던 문제.
- 검색 결과에서 제목 없는 일기의 'Untitled'가 가장 굵고 진한 글자라 정작 일기 내용보다
  눈에 띄던 문제 → '제목 없는 일기'를 흐리게.
- 미리보기가 90자에서 단어 중간을 자르던 문제 → 줄 단위로 자연스럽게.
- 감정 칩을 선택하면 얼굴 아이콘이 왼쪽으로 밀리던 문제 (체크가 같은 줄에 들어가면서).
- **'피곤함' 감정에 눈이 X자인 아이콘**이 쓰여 기절/사망처럼 읽히던 문제 → 잠 아이콘으로.
  '감사'의 뽀뽀 얼굴도 하트 아이콘으로 바꿨습니다.
- 같은 폼 안에서 '오늘의 감정'·'사진'만 다른 크기라 섹션 제목이 작다→크다로 튀던 문제.
- 달력에서 기록 없는 날의 안내가 한 줄짜리 문장이라 할 수 있는 일이 없던 문제 →
  다른 화면과 같은 빈 상태 + 대화하러 가기 버튼.
- 되돌릴 수 없는 '계정 및 데이터 삭제'가 로그아웃과 똑같이 보이던 문제 → 아이콘과 안내 추가.

## [0.7.4] - 2026-08-19

전 화면을 다시 캡처해 하나씩 검토하며 찾은 것들입니다.

### 수정

- **휴지통의 "영구 삭제" 글자가 배경에 묻혀 있었습니다** (대비 2.5:1). 노란색으로 바꿀 때
  버튼 글자색을 어두운 잉크로 통일했는데, 빨간 삭제 버튼까지 그 색을 쓰면서 어두운 글자가
  어두운 빨강 위에 올라갔습니다. 위험 버튼 전용 글자색을 따로 두고 대비 테스트를 추가했습니다.
- **비활성 버튼의 글자가 안 보이던 문제.** 흐리게 만드는 방식이라 옅은 노랑 위 금색 글씨가
  2:1대까지 떨어졌습니다. 색을 바꾸는 방식으로 고쳐 "지금은 못 누른다"는 유지하고 가독성은
  지켰습니다. 대화 화면의 전송 버튼이 아이콘 없는 빈 버튼처럼 보이던 것도 같은 원인이었습니다.
- 설정의 토글 손잡이가 청록색으로 나오던 문제 (웹 기본값이 팔레트를 무시했습니다).
- 입력창에 브라우저 기본 검정 포커스 링이 브랜드 테두리 위에 겹쳐 그려지던 문제.
- **한국어가 어절 중간에서 줄바꿈되던 문제** — "내가 선택한 친구 / 에게만"처럼 끊겼습니다.
- 옅은 노랑 버튼(secondary)에 테두리가 없어 크림 배경에서 버튼으로 안 보이던 문제.
- 통계의 기록 잔디가 카드 폭의 60%만 채워 잘린 것처럼 보이던 문제.
- 다크 모드에서 잔디의 '오늘' 칸 테두리가 노란 칸 위 노란 테두리라 안 보이던 문제.
- 통계 카드 5개 중 마지막 하나만 혼자 전체 폭으로 늘어나 격자가 깨지던 문제.
- '의견 보내기'가 아무도 받지 않는 자리표시자 주소로 메일 앱을 열던 문제 → 저장소 이슈로.
- 앱 정보 화면에 버전이 두 번 나오고, 개발용 값(환경: development)이 노출되던 문제.
- 연습 화면이 안내만 하고 갈 곳이 없는 막다른 화면이던 문제 → 대화로 가는 버튼 추가.
- '다시 말해보기'와 '다시 말해 보기'로 띄어쓰기가 갈리던 문제.
- 홈 인사말 아래 두 줄이 붙어 한 문단처럼 읽히던 문제.
- 친구 화면 안내에서 기술 용어를 빼고, 지금도 가능한 일기 공유 방법을 알려 줍니다.
- 일기 상세의 공개 범위 안내가 해요체와 합쇼체를 섞어 쓰던 문제.

## [0.7.3] - 2026-08-19

전 화면을 캡처해서 하나씩 뜯어보다 찾은 것들입니다.

### 수정

- **홈 화면에 "NaN/"이 찍히던 문제.** 예전부터 앱을 쓰던 분들에게만 보였습니다.
  저장된 설정과 기본값을 합칠 때 최상위만 얕게 덮어쓰고 있어서, 나중에 추가된 항목
  (하루 목표 문장 수)이 통째로 비어 있었습니다. 그룹 안쪽까지 합치도록 고쳤고,
  값이 비어 있어도 화면에 NaN이 안 나오게 이중으로 막았습니다.
- **일부 화면에서 글자만 세리프(명조)로 나오던 문제.** `/write/chat`,
  `/diary/...` 처럼 한 단계 깊은 주소에서 폰트·manifest·서비스 워커 파일 경로가
  전부 404였습니다. 상대 경로(`./fonts/...`)를 써서 `/write/fonts/...` 를 찾고
  있었습니다. **홈 화면에 추가할 때 manifest를 못 읽어 앱 이름이 엉뚱하게 잡히던
  것도 여기서 왔을 수 있습니다.** 전부 절대 경로로 바꿨습니다.
- 폰트를 못 받았을 때 브라우저 기본값(Times 명조)으로 떨어지던 것 — 한글은 고딕인데
  영어만 명조로 나오는 화면이 됐습니다. 폴백 글꼴을 지정했습니다.
- **단어장에서 "저장한 표현 (4)"라고 써 놓고 목록이 비어 있던 문제.** 제목은 전체
  개수를 세는데 목록은 오늘 복습할 표현을 빼고 그리고 있었습니다.
- 화면 제목(상단 헤더)과 탭 라벨이 SUIT가 아닌 시스템 폰트로 나오던 문제.
- 친구 코드가 옛 이름에서 온 `MLW-`로 시작하던 문제 → `DLOG-`. 이미 만들어진
  코드는 앱을 켤 때 자동으로 바뀝니다 (아직 친구 기능이 동작하지 않아 안전합니다).

### 추가

- 빌드가 상대 경로 주입을 잡아내고 실패하도록 검사를 넣었습니다. 화면이 멀쩡해
  보여서 놓치기 쉬운 종류의 버그였습니다.
- 설정 병합 회귀 테스트 — 설정 항목을 새로 추가할 때 NaN 버그가 되돌아오면
  테스트가 먼저 깨집니다.

## [0.7.2] - 2026-08-19

### 수정 (앱 이름이 계속 옛 이름으로 보이던 문제 — 원인은 서비스 워커였습니다)

앱 이름을 D-log로 바꾸고 배포했는데도 홈 화면에 옛 이름이 남고, **지웠다 다시 설치해도
그대로**인 문제가 있었습니다. 캐시 문제라고 안내드렸지만 실제 원인은 이쪽 버그였습니다.

- 서비스 워커가 `manifest.json`을 **캐시 우선**으로 서빙하고 있었습니다. manifest는
  앱 이름과 아이콘의 원본인데, 홈 화면에 추가할 때 폰이 읽는 게 바로 이 파일입니다.
  옛 워커가 옛 manifest를 계속 돌려주니 몇 번을 다시 설치해도 옛 이름이 박혔습니다.
  → `manifest.json`도 **네트워크 우선**으로 바꿨습니다
- 새 서비스 워커가 모든 창이 닫힐 때까지 기다리느라 옛 워커가 계속 응답하던 것도
  같은 문제를 키웠습니다 → 설치 즉시 교체되도록(`skipWaiting`) 바꿨습니다

### 추가

- 설정 → 지원에 **"앱 파일 새로 받기"** 버튼. 이미 옛 파일을 붙들고 있는 분들이 직접
  풀 수 있는 방법입니다. 서비스 워커 등록과 파일 캐시만 지우고 다시 받습니다 —
  **일기와 설정은 지워지지 않습니다** (다른 저장소에 있습니다).

## [0.7.1] - 2026-08-19

### 추가

- 홈 화면에 옛 이름(Mellow Diary)이 남아 있는 분들을 위한 안내 카드. PWA를 설치하면
  폰이 그 시점의 이름·아이콘을 복사해 두기 때문에, 앱을 업데이트해도 홈 화면 라벨은
  그대로 남습니다 (특히 iOS는 아예 바뀌지 않습니다). 서버에서 고칠 수 있는 문제가
  아니라서 앱이 직접 알려 주고 바꾸는 방법을 안내합니다.
  홈 화면에서 실행 중일 때만 뜨고, 한 번 닫으면 다시 뜨지 않습니다.

### 문서

- 저장소 키(`mellow-*`)에 "바꾸면 사용자 데이터가 전부 사라진다"는 경고 주석을 달았습니다.
  화면에 나오지 않는 내부 값이고, 이름을 정리하려다 일기를 날리는 일을 막기 위한 것입니다.

## [0.7.0] - 2026-08-19

### 추가 (한국어 폰트)

- **SUIT**를 한국어 UI 폰트로 번들했습니다. 그동안 커스텀 폰트를 쓰는 건 영어 일기
  본문(Lora)뿐이었고 한국어는 기기 기본 폰트여서, 화면의 90%를 차지하는 한국어가
  기기마다 다르게 보이고 브랜드 개성도 없었습니다
- 라이선스 SIL OFL 1.1(상업적 사용·번들 가능), Regular/SemiBold/Bold 세 웨이트만
  포함해 **498 KiB**. 원본 그대로 쓰므로 family name도 `SUIT`를 유지합니다
- 외부 CDN을 호출하지 않고 로컬 woff2만 씁니다. 서비스 워커가 미리 받아 두므로
  오프라인에서 처음 열어도 글자가 시스템 폰트로 튀지 않습니다

### 변경 (폰트 규칙을 언어별 → 역할별로)

이전에는 "영어는 세리프, 한국어는 산세리프"였는데, 교정 카드처럼 두 언어가 나란히
붙는 화면에서 폰트가 따로 놀았습니다. 이제 **콘텐츠의 역할**로 나눕니다.

| | 폰트 |
|---|---|
| 조작·학습 영역 (버튼·대화·교정 카드·설정) | SUIT — 영어 문장도 포함 |
| 완성되어 보관되는 일기 | 영어 Lora / 일본어 플랫폼 명조 |

그래서 "대화가 일기로 완성됐다"는 변화가 폰트로도 드러납니다.

- 타입 스케일 정리: 실제 크기를 13/14/16/18/22/26/30 일곱 단계로 (일기 24는 예외).
  21→22, 17→18 — 각각 위아래 단계와 차이가 잘 안 보였습니다
- 큰 글자에만 자간을 살짝 좁혔습니다. 한글은 16px 이하에서 음수 자간을 크게 주면
  다크 모드·저해상도 화면에서 획이 뭉칩니다
- 웨이트 500 제거 (`bodyMedium` → `bodyStrong` 600)
- 교정 문장 17/28/500 → 16/28/600 — 크기보다 줄간격과 굵기로 구분합니다
- 일본어 일기는 줄간격을 1px 더 줍니다 (명조가 세로 획과 밀도가 높아서)
- 영어 일기 본문 Lora 500 → 400 (본문에는 Regular가 맞습니다)

### 수정

- 입력창 글자만 시스템 폰트로 튀던 문제 — `TextInput`은 `AppText`를 거치지 않아
  폰트를 직접 지정해야 합니다
- 빌드에 폰트 파일이 빠지면 조용히 시스템 폰트로 떨어지던 것을, 빌드가 실패하도록
  검사를 추가했습니다

## [0.6.0] - 2026-08-19

### 변경 (앱 색을 노랑으로)

- 브랜드 색을 보라 → **노랑**으로 바꿨습니다. 생노랑이 아니라 살짝 가라앉힌
  꿀색(#E0A82E)이라 눈이 편하고, 크림색 종이 배경과 잘 붙습니다
- 아이콘도 같은 노랑으로 다시 만들었습니다 (편지 + 펜촉은 그대로)
- 라이트/다크 모두 새로 잡았습니다

### 노랑을 쓰면서 함께 처리한 것

노랑은 밝아서 밝은 배경 위 글자로 쓰면 대비가 2:1대까지 떨어집니다. 색만 바꾸면
안 보이는 글자가 생기기 때문에 브랜드 색을 셋으로 나눴습니다.

| 토큰 | 쓰는 곳 | 왜 |
|---|---|---|
| `primary` | 버튼·칩 **배경** | 이 위에는 어두운 잉크 글자를 올립니다 (7.1:1) |
| `primaryInk` | **글자·아이콘**·포커스 링 | 크림 배경 위에서 5.2:1로 읽힙니다 |
| `primaryBorder` | 선택 상태 **테두리**·진행 바 | 밝은 배경에서 노란 면의 경계가 사라지는 걸 막습니다 |

- 경고색을 브랜드 노랑과 헷갈리지 않게 주황·적갈 쪽으로 옮겼습니다
- **색 대비 회귀 테스트 66개 추가** (`src/theme/__tests__/contrast.test.ts`) —
  나중에 색을 손보다 글자가 안 보이게 되면 테스트가 먼저 잡아냅니다
- 실제 화면(온보딩·로그인·홈)을 브라우저로 띄워 눈으로도 확인했습니다

### 수정

- 로그인 화면에 남아 있던 "AI는 Mock으로 동작해요" 문구를 실제 동작에 맞게 수정

## [0.5.0] - 2026-08-19

### 변경 (앱 이름과 아이콘)

- 앱 이름 **Mellow Diary → D-log**. 브이로그가 하루를 영상으로 남기듯,
  하루를 외국어 문장으로 남기는 데일리 로그라는 뜻입니다
- 새 아이콘: **편지 + 펜촉** — "말한 하루가 한 통의 편지가 된다".
  기존 아이콘은 Expo 기본 템플릿(파란 A)이라 앱과 아무 관계가 없었습니다
- 아이콘을 이미지가 아니라 **코드로 관리**합니다 (`scripts/generate-icons.py`).
  색이나 형태를 바꾸면 iOS·Android·PWA용 9개 파일이 한 번에 다시 만들어집니다
- 로그인·소개·온보딩 화면의 브랜드 아이콘을 새 아이콘과 같은 편지 모양으로 통일
- 쓰지 않던 Expo 템플릿 이미지(react-logo, expo-badge, tabIcons 등) 정리

### 유지 (데이터는 그대로입니다)

- **기존 일기·단어장·설정은 그대로 남습니다** — 저장소 키를 바꾸지 않았습니다
- 이름을 바꾸기 전에 내보낸 백업 파일도 **계속 복원됩니다**
  (`mellow-diary-backup` 형식을 계속 읽습니다)
- 앱 주소가 그대로라 폰에 설치된 PWA는 재설치 없이 업데이트됩니다.
  다만 홈 화면의 앱 이름은 기기에 따라 바로 안 바뀔 수 있어요 — 그때는 지웠다
  다시 추가하면 D-log로 보입니다

## [0.4.1] - 2026-08-19

### 변경 (가장 큰 변화 — 내장 AI가 진짜 AI가 되었습니다)

- **영구 무료(0원) 확정**: 외부 AI API를 연결하지 않습니다. 나중에 "무료 한도를 넘어서
  멈추는" 구조를 아예 만들지 않았습니다. 기기 안에서 도는 규칙 엔진이 이 앱의 정식
  교정 엔진입니다 — 인터넷이 없어도 동작하고, 쓴 문장이 기기 밖으로 나가지 않습니다
- 교정 규칙 **88개**: 시제 15 · 수 일치 14 · 관사 14 · 전치사 17 · 자연스러움 16 ·
  일본어 12 (+ 한국어 사용자가 자주 만드는 오류 6)
- 한 문장에서 **여러 규칙이 함께** 적용됩니다 (이전에는 문장당 하나만 고쳤습니다)
  - 예: "I ate sandwich and listened music at morning."
    → "I ate a sandwich and listened to music in the morning."
- 오탐(맞는 문장을 틀렸다고 고치는 것) **59건을 찾아 수정**했습니다. 맞는 문장은
  건드리지 않는 것을 가장 중요하게 봤습니다
- 대화 주제 사전 10개 → **20개**, 같은 대화에서 같은 답을 반복하지 않습니다
- 휴대폰·음성 입력이 흘린 아포스트로피를 조용히 되돌립니다("don t" → "don't").
  입력 문제이지 영어 실력 문제가 아니라서 교정 카드에 띄우지 않습니다
- 같은 오류가 한 일기에 여러 번 나와도 **모두** 고칩니다
  - 예: "I listened music and my friend listened music too."
    → "I listened to music and my friend listened to music too." 

### 추가

- **일기 공유** (서버 없이, 0원): 일기 상세 → 더보기 → 일기 공유하기 →
  카톡·메모 등으로 내보내기. 지원하지 않는 환경에서는 클립보드 복사로 대체.
  사진은 공유에 포함하지 않습니다
- **백업 내보내기 / 가져오기**: 데이터가 기기에만 있으므로 설정 → 계정에서 직접
  백업하고 새 기기에서 복원할 수 있습니다 (기존 일기를 덮어쓰지 않고 합칩니다)
- 비용 안전장치 테스트: 유료 경로가 실수로 기본값이 되거나 배포 파이프라인에서
  켜지는 것을 막습니다

### 제거

- Mock AI 표기 — 자리표시자가 아니라 정식 엔진이므로 "내장 AI"로 통일
- 최종 일기의 가짜 한국어 번역 문구 — 규칙 엔진으로는 번역할 수 없어서, 그럴듯한
  가짜 번역을 넣는 대신 비워 둡니다 (교정 설명은 계속 한국어로 나옵니다)

### 테스트

- 268개 통과 (이전 72개). 오탐 회귀 코퍼스(맞는 문장 90여 개)와 교정 회귀 테스트를
  함께 돌려, 규칙을 넓힐 때 맞는 문장이 깨지지 않는지 자동으로 확인합니다

## [0.3.0] - 2026-08-18

### 추가 (언어 학습 앱들의 장점을 우리 방식으로)

- 오늘의 목표 (듀오링고식 daily goal): 하루 목표 문장 수(설정에서 1/3/5/10) + 홈 진행 바
  "오늘 3문장 중 2문장을 이야기했어요" — 미달성 압박 문구 없음
- 너그러운 연속 기록 (스트릭 프리즈식): 7일마다 하루는 쉬어가도 기록이 이어지고
  "(하루 쉬어가도 이어져요)"로 정직하게 표시
- 단어장 퀴즈 복습 (말해보카식): 4지선다 뜻 맞히기 — 다른 저장 표현의 뜻이 오답 보기,
  정답 시 복습 간격 증가 / 오답 시 "내일 다시 만나요", 발음 자동 재생
- 표현 숙련 단계 배지: 새 표현 → 익히는 중 → 익숙함 (복습 횟수 기반)
- 다시 말해보기 연습 (Speak식): 지난 7일 교정문을 모아 듣기 → 따라 말하기 →
  완료 체크. 홈에 "연습 N문장이 기다리고 있어요" 카드
- 기록 잔디: 학습 통계에 최근 12주 활동 히트맵 (색+테두리 구분, 스크린리더 라벨)
- 단위 테스트 16개 추가 (목표 집계, 쉬어가기 경계, 퀴즈 보기 생성) — 총 72개

## [0.2.0] - 2026-08-18

### 추가 (디자인 시스템 전면 개편 — "종이 일기장 + 편지지" 콘셉트)

- 새 팔레트 (WCAG AA 대비 충족): primary #715A9B, 시맨틱 토큰(scrim, focusRing,
  inputBackground, diaryPaper, pressedBackground 등), 라이트/다크 완전 대응
- 타이포그래피 확장: display/bodyMedium/label/diaryTitle/diaryBody/correctionSentence
- 영어 일기 세리프 폰트 Lora 번들 (무료), 일본어 일기는 플랫폼 세리프 사용
- 기능 이모지 아이콘 전면 교체 → Feather 라인 아이콘 (@expo/vector-icons, 무료)
- 감정 선택 6종 정리 + 얼굴 아이콘/라벨/체크 (색상만으로 구분하지 않음)
- DiaryPaper: 최종 일기·일기 상세를 "한 장의 종이"로 표시 (완성 시 fade 모션, Reduce Motion 대응)
- 홈 개편: 배경 위 인사말 + "오늘의 편지" 대표 카드 (다른 질문 보기), 일기 리스트 행 구조
- AI 로딩: 잉크 획 3개 애니메이션 + 상황별 문구 ("이야기를 잘 듣고 있어요" 등)
- 교정 카드 개편: 바뀐 부분 diff 표시(취소선/밑줄+아이콘), 접히는 설명("왜 이렇게 말해요?"),
  주요 액션 2개 + 더보기 메뉴
- finalize에 "오늘의 교정 모아보기" — 교정 타이밍 '대화 끝난 후' 설정 실제 지원
- 탭바 개편: 선택 상태 표시선 + 라벨 굵기, 중앙 쓰기 버튼 56px, safe area 대응
- 달력: 셀 44px+ 터치 영역, 이전/다음 달 접근성 라벨, 감정 아이콘

### 수정 (전면 리뷰 워크플로에서 확정된 버그)

- 자정이 지나면 어제의 진행 중 대화에 접근할 수 없던 문제 — 날짜와 무관하게 이어하기 가능
- 일기 '생성' 시점에 대화가 잠겨 저장 없이 이탈하면 복구 불가하던 문제 —
  저장 완료 시점에만 대화를 마무리, 뒤로 가면 "만들어 둔 일기로 돌아가기" 제공
- "교정문 적용"이 실제로 반영되지 않던 문제 — 내 메시지 교체 + 최종 일기 자연스러운
  버전과 연습 문장에 교정문 반영
- 인사만 남은 빈 대화가 매일 누적되던 문제 (자동 정리)
- 렌더 중 사용량 스토어가 변경될 수 있던 문제
- iOS 다크 테마에서 밝은 키보드가 뜨던 문제 (keyboardAppearance)
- 사용자 화면의 개발 용어(Supabase/Phase 등) 제거

## [0.1.3] - 2026-08-18

### 추가

- AI 대화 중 뒤로가기 시 확인 팝업: "일기 만들고 저장하기 / 나가기(대화 보관) / 계속 이야기하기"
- 대화 이어하기: 나가도 대화가 사라지지 않고, 오늘 홈의 "진행 중인 AI 대화" 카드에서
  마지막 이야기 미리보기와 함께 이어서 대화 가능

### 수정

- 웹 빌드 시 Metro 캐시로 구버전 정보가 번들에 남는 문제 수정 (build:web에 --clear 추가)
  — 업데이트 배너가 잘못 표시될 수 있는 잠재 버그 예방

## [0.1.2] - 2026-08-18

### 추가

- Mock AI가 사용자 발화의 키워드(더위, 피곤함, 영상, 음식, 친구, 일/공부, 감정, 날씨 등)를
  인식해 관련된 공감·질문으로 답변 (고정 로테이션 제거)
- 주제 미감지 시 사용자의 핵심 단어를 인용해 질문하는 fallback
- 교정 규칙 추가: 주어 없는 형용사 시작 문장 ("so tired because very hot" →
  "I'm so tired because it's very hot.")

### 수정

- TTS가 이모지를 소리 내어 읽는 문제 수정 — 음성 재생 전 이모지/기호 제거
  (예: "Hi! 😊" 재생 시 "smiling face"를 읽지 않음)

## [0.1.1] - 2026-08-18

### 추가

- PWA 설치 지원: manifest.json, 앱 아이콘(180/192/512), 서비스 워커(오프라인 shell)
- 앱 내 업데이트 확인: 새 버전이 배포되면 오늘 홈에 "업데이트가 필요해요" 배너 자동 표시
- 설정 → 지원에 "업데이트 확인" 버튼 (현재/최신 버전 비교, 즉시 업데이트)
- Web/PWA는 재설치 없이 버튼 한 번으로 업데이트 (서비스 워커 갱신 + 새로고침)
- GitHub Pages 자동 배포 워크플로 (push 시 lint/test/build 후 배포)
- 빌드 후처리 스크립트: version.json 생성, SW 버전 치환, SPA 404 처리

### 변경

- build:web 스크립트가 PWA 후처리를 포함하도록 변경
- app.config.js 도입 (GitHub Pages 하위 경로 배포용 baseUrl 환경변수 지원)

## [0.1.0] - 2026-08-18

### 추가

- 프로젝트 초기화 (Expo SDK 57, TypeScript strict, Expo Router)
- 감성 디자인 시스템: 테마 토큰(크림/라벤더/로즈/세이지), 다크모드, 시스템 테마 연동, 폰트 확대
- 온보딩: 닉네임, 학습 언어(영어/일본어), 실력, 목적, 교정 강도/타이밍, 말하기 속도
- Demo 로그인 (계정 없이 로컬 저장 체험) + Google/Apple/이메일 로그인 구조(설정 필요 안내)
- 오늘 홈: 오늘의 질문, 연속 작성일, 작성 중 임시 저장 복구, 복습 알림
- 직접 일기 쓰기: 자동 임시 저장, 감정/태그, 사진 추가(압축 + EXIF 위치 정보 제거), 글자 수 제한
- Mock AI 대화: 티키타카 대화, 교정 카드(원문/교정문/한국어 설명/핵심 표현), 교정 타이밍 설정 반영
- 교정 문장 다시 말하기: STT(Web Speech API) + 텍스트 fallback, 목표 문장 일치도(참고용 지표)
- 기기 TTS(expo-speech): 영어/일본어, 속도 조절, 천천히 듣기, 문장 분할 재생
- 대화 → 최종 일기 완성: 제목 후보, 쉬운/자연스러운/원문/직접 수정 버전 선택, 사용자 승인 후 저장
- 달력(감정 표시, 월 이동, 하루 여러 일기), 타임라인, 일기 상세/수정, 검색(debounce), 즐겨찾기
- 단어장 + 간단한 간격 반복 복습, 학습 통계(측정 가능한 값만)
- 휴지통(soft delete + 보관 기간), 데이터 내보내기(JSON), 계정/데이터 삭제
- 세분화된 설정: 계정/학습/음성/일기/디자인/알림/개인정보/개발
- AI 사용량 제한(일일 대화 20턴, 일기 완성 3회 기본값, 환경변수로 조절) 및 한도 초과 시 안내
- AI Provider Adapter (Mock / Anthropic 서버 경유) + Zod 응답 스키마 검증 + fallback
- Supabase 마이그레이션(23개 테이블) + 전체 RLS 정책 + Storage 정책
- Anthropic Edge Function (서버 전용 키, 사용량 재검증, idempotency)
- 버전 관리 스크립트(version:patch/minor/major), 앱 하단 버전 표시
- 단위 테스트 39개 (문장 정규화/유사도, 날짜/시간대, 사용량 제한, AI 스키마, Mock Provider)

### 보안

- API 키 클라이언트 노출 금지 구조 (.env.example 문서화)
- 일기 기본 비공개, 음성 원본 기본 미저장, 사진 EXIF 제거
- RLS: 본인 데이터만 접근, 공유/차단/댓글 권한 정책
